<!doctype html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <title>{{ title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Brisbane's only social chess club" />
    <link rel="stylesheet" href="{{ '/assets/style.css' | url  }}" />
    <link rel="icon" href="{{ '/assets/favicon.png' | url  }}" />
    <link rel="apple-touch-icon" href="{{ '/assets/apple-touch-icon.png' | url  }}" />
    <meta name="generator" content="{{ eleventy.generator }}" />
  </head>
  <body>
    <div class="background-overlay">
      <main class="content-wrapper">
        <!-- Logo -->
        <div class="logo">
          <a href="{{ '/' | url  }}">
            <img src="{{ '/assets/logo.jpg' | url  }}" alt="Brisbane Social Chess Logo" />
          </a>
          <h1>Brisbane Social Chess</h1>
          <p>A place for casual chess, for fun and friendly people.</p>
        </div>

        <!-- Top Navigation -->
        <nav class="nav-links">
          <a href="{{ '/' | url  }}" class="{% if page.url == '/' %}active{% endif %}">Home</a>
          <a href="{{ '/about/' | url  }}" class="{% if page.url == '/about/' %}active{% endif %}">About</a>
          <a href="{{ '/games/' | url  }}" class="{% if page.url == '/games/' %}active{% endif %}">Games</a>
          <a href="{{ '/meetup/' | url  }}" class="{% if page.url == '/meetup/' %}active{% endif %}">Meetups</a>
          <a href="{{ '/minutes/' | url  }}" class="{% if page.url == '/minutes/' %}active{% endif %}">Minutes</a>
          <a href="{{ '/register/' | url  }}" class="{% if page.url == '/register/' %}active{% endif %}">Register</a>
          <a href="{{ '/rules/' | url  }}" class="{% if page.url == '/rules/' %}active{% endif %}">Rules</a>
          <a href="{{ '/terms/' | url  }}" class="{% if page.url == '/terms/' %}active{% endif %}">Terms</a>
          <a href="{{ '/contact/' | url  }}" class="{% if page.url == '/contact/' %}active{% endif %}">Contact</a>
        </nav>

        <!-- Content -->
        {{ content | safe }}
      </main>
    </div>

    <!-- Footer -->
    <footer class="site-footer">
      <p>&copy; <span id="year"></span> Brisbane Social Chess. All rights reserved.</p>
    </footer>
    <script type="text/javascript" src="{{ '/assets/script.js' | url  }}"></script>
  </body>
</html>
